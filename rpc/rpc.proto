syntax = "proto3";

package example;

service ServiceTest {
    rpc SayHello(Req) returns (Rsp) {};
    rpc Crazy(stream Req) returns (stream Rsp) {};
}


message Req {
    string name = 1;
}

message Rsp {
    uint32 code = 1;
    string resp = 2;
}